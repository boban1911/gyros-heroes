
node_modules
dist
dist-ssr
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/
.vercel
# Anything env-shaped except a checked-in example. Both `.env.local` (vercel
# env pull output) and `.env.preview-loyalty` (vercel env pull --environment
# preview) contain real secrets — don't commit either.
.env
.env.*
!.env.example

# Claude Code agent worktrees + hook state
.claude/worktrees/
.claude/.claudemd-eval-state
